{
  "name": "receipts-register-mobile-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client/nextjs",
    "expo"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }

}
